# Replit setup

## Run

The app is a static Express server. Start it with:

```bash
npm run dev
```

The Replit workflow serves the app on port 5000 for the web preview. The server
also accepts a `PORT` environment variable and defaults to 5000.
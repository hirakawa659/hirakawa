# Novel Editor Persistent Rules & Guidelines

## 1. 3つの責務分離の厳守ルール（Production / Preview / External）

今後のすべての機能追加・修正・リファクタリングにおいて、以下の3責務を厳格に分離して実装・管理すること。

1. **01-production（実際のサイト・本番機能）**:
   - ユーザーが使用するエディタ本体、原稿用紙グリッド描画、動的文字数計算、ローカル保存・復元、設定、各種編集機能、および **Supabaseクライアント認証・同期処理** はすべてここに配置・維持する。
   - **Preview非依存**: Preview用のモックコード、特定文字数の決め打ち、画面枠調整用の一時コードをProductionロジックに混入させてはならない。

2. **02-preview（AI Studioプレビュー・表示検証）**:
   - AI Studio上の表示確認、各種端末幅（375px〜1440px）でのレイアウト崩れ検証、マルチ列数（18〜40文字）のテストハーネスはここに分離する。
   - **状態非汚染**: Preview側の操作・検証がProduction側のユーザー原稿データ、`localStorage`、設定値を破壊・上書きしないサンドボックス構造を維持する。

3. **03-external（外部サービス・インフラ）**:
   - Supabaseサーバー側のテーブルスキーマ（PostgreSQL DDL）、RLSセキュリティポリシー、外部CDNライブラリ、外部API契約などの定義を整理・管理する。
   - Supabaseのサービス自体はExternalであるが、Supabaseと通信するクライアント認証・同期コードは必ずProduction側で管理する。

---

## 2. 文字数設定・グリッド計算の動的性維持ルール（30文字について）

1. **30文字は初期デフォルト値（フォールバック）**:
   - 「30」という値は初期起動時や未設定時のデフォルト値としてのみ扱い、レイアウト計算の固定値・特殊値・基準値としてハードコードしないこと。
2. **動的適応の保証**:
   - 18〜40文字（最大100文字）まで、ユーザーが選択した文字数（29, 28, 25, 20, 18など）に `--cs` 変数およびグリッド計算が完全に追従する設計を維持し、勝手に30へ戻す処理を行わないこと。

---

## 3. プレビュー消滅・バグ防止ルール（HTML/バニラJS・原稿用紙エディタ用）

1. **エディタの高さを最低限確保**:
   - テキスト入力エリア（contenteditable）や親要素（`#w`など）に文字が入っていない初期状態でも背景のマス目が潰れて消えないよう、必ず `min-height: 400px;` などの最小の高さをCSSで確保してください。

2. **文字とグリッド線の同期**:
   - マス目（`linear-gradient` による背景）の縦横サイズ（`background-size`）は、CSS変数（`--cs`）などを通じてフォントサイズ（`font-size`）や行の高さ（`line-height`）と必ず連動するように計算・設定し、プレビューの画面サイズが変わってもマス目と文字が絶対にズレないようにしてください。

3. **プレビュー・印刷時の背景保持**:
   - プレビュー表示や画面切り替えの際に、背景のマス目がブラウザの仕様で省略されないよう、CSSに `-webkit-print-color-adjust: exact;` および `print-color-adjust: exact;` を必ず明記してください。

---

## 4. モジュール責務分離・コード保守ルール（src/構成の厳守）

今後の開発・改修において、以下のモジュール構成と責務境界を必ず維持すること。

1. **ディレクトリ責務**:
   - `src/editor/` (`grid.js`, `cursor.js`, `editor.js`): 入力処理、原稿用紙マス目・動的文字数・行番号、カーソル・全角スペース補正、隠し文字/半角変換
   - `src/data/` (`storage.js`, `migration.js`, `backup.js`): `localStorage` 保存、履歴管理（Undo/Redo）、ツリー階層修復・並び替え、UTF-8 BOM付txt/ネイティブZIPバックアップ
   - `src/cloud/` (`auth.js`, `sync.js`): Supabase 認証・ログイン/登録/パスワード再設定、自動/手動クラウド同期・タイムスタンプ管理
   - `src/management/` (`documents.js`, `trash.js`): 原稿・フォルダのツリー階層、作成・複製・削除、ゴミ箱の復元・完全削除
   - `src/ui/` (`settings.js`, `dialogs.js`): ツールバー並び替え、カスタムラベル、配色テーマ、モーダルダイアログ
   - `src/utils/` (`text.js`, `dom.js`): 文字列正規化・エスケープ、会話文抽出、DOM補助

2. **依存方向とレイヤー境界**:
   - **エディタやUIから直接Supabaseや外部通信を呼び出さない**: クラウド同期が必要な場合は `src/cloud/sync.js` 経由で行う。
   - **巨大な単一ファイルへの逆戻り禁止**: 新規ロジック追加時は適切な責務のモジュールへ配置し、`index.html` や単一スクリプトにすべてのロジックを詰め込まない。

3. **機能・互換性の保守**:
   - 既存のUI・デザイン・操作感・原稿データ形式を無断で変更・破壊しない。
   - すべての変更において段階的な動作確認を行い、安定性を最優先する。

---

## 5. Supabase 接続設定の方針

- すでにプロジェクト内で設定されている `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY` （あるいは `src/cloud/auth.js` 等に定義済みの既存設定）をそのまま引き継いで使用する。
- 新しい環境変数を作成したり、値を勝手に推測・変更したりしないこと。

---

## 6. 最重要方針・開発ポリシー

- **既存機能・既存UI・既存データを壊さないことを最優先**とする。
- 大規模なリファクタリング、勝手なUI変更、不要な最適化、新しいライブラリの追加などは、明確な指示がない限り行わない。
- 問題を見つけた場合:
  1. 原因を調査
  2. 実測・確認
  3. 最小限の修正案を提示
  4. 修正
  5. 実機・既存機能で確認

---

## 7. 対応端末について（iPad / iPad Air + Safari の重視）

- **iPad / iPad Air + Safari** を重要な実利用環境として意識し、PCブラウザだけでなくiPad Safariでの動作を確認すること。
- 特に以下を慎重に扱う:
  - タッチ操作 / タップ・長押し / テキスト選択 / contenteditable
  - 日本語IME / キーボード表示時のレイアウト / スクロール
  - モーダル / select / option / pointer / mouse / touchイベント / user-select
  - viewport・画面サイズ / Safari固有の挙動 / DOMの生成タイミング

---

## 8. 文字数設定に関する注意点

- 文字数設定モーダルの `#colsModalSelect` 等のDOM生成タイミングと初期化順序を必ず確認し、DOMが存在しない状態で初期化されないようにすること。
- select変更時に古い設定値で勝手に上書きする競合を起こさない。
- 18〜40文字の選択肢は二重管理せず共通の初期化処理を利用する。

---

## 9. パフォーマンス方針

- 行番号表示は削除済み。
- `ensureGridLines()` や `autoFormatEditor()` 等について、実測で問題が確認されるまでは勝手に最適化・変更しない。「理論上重そう」という理由だけで既存処理を変更しない。

---

## 10. データ・保存・同期

- 既存のローカル保存・クラウド同期・バックアップ・競合対策を壊さない。
- 入力中に不要なネットワーク通信を増やさない。既存データとの互換性を必ず維持する。

---

## 11. 開発時の基本姿勢

- **「動いているものは理由なく触らない」**
- **「まず計測・確認、次に最小修正」**
- **「PCで動けばOKではなく、iPad Air / Safariで実際に使えることを重視する」**

---

## 12. 対話・応答スタイル（トークン節約ルール）

- **ですます調（敬体）は不要**。だ・である調または箇条書き・簡潔な常体で回答する。
- トークン節約のため、挨拶・前置き・過度な説明・お世辞・自己評価は省き、要点と結論、変更内容のみを極力シンプルに伝える。


